import { Metadata } from "next";

export const metadata: Metadata = {
  title: "grosse-time | Login",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
